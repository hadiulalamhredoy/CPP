#include <stdio.h>
#include <math.h>

int main() {
    double r;
    scanf("%lf", &r);

    double area = M_PI * r * r;       // πr²
    double circumference = 2 * M_PI * r; // 2πr

    printf("%.6f %.6f\n", area, circumference);

    return 0;
}
