#include <stdio.h>

int main() {
    long long a, b;
    scanf("%lld %lld", &a, &b);

    long long d = a / b;     // integer division
    long long r = a % b;     // remainder
    double f = (double)a / b; // floating-point division

    printf("%lld %lld %.5f\n", d, r, f);

    return 0;
}
