//Declaration
umm<int,int>umm;
//insert()
umm.insert(10);
//begin() and end()
for(auto it = umm begin();it = umm.end();it++)
{
    cout<<(*it).first<<""<<(*it).second<<endl;
}
//bucket_count()
umm.bucket_count(4)<<endl;
//bucket_size()
umm.bucket_size(4)<<endl;
//count()
int cnt = umm.count(10);
count<<cnt<<endl;
//clear()
umm.clear()
//size()
cout<<umm.size()<<endl;
//Find()
if(it!=ummend() <<cout<<" Find"<<endl;
else cout<<"Not Find"<<endl;
