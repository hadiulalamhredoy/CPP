//Decaration 
order_of_key(k)
find_by_order(k)
#include<bits/stdc++.h>
using namespace std;
ordered set s;
//insert
s.insert(10);
//order of key & find by order
int ans = s.order of key(3);
auto it = s.find by order(2);
cout<<*it<<endl;
